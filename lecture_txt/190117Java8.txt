자바 8일차 강의
---------------------------------------------------------------------------------------------------------------------------------------
메소드의 가변인자

ex)
public int sum(int ... numbers){
  int sum = 0;
  for(int n : numbers){
    sum += n;
  }
}

이때 numbers는 배열로 처리됨

가변인자는 항상 매개변수 순서의 맨 마지막에 선언해야 한다.
---------------------------------------------------------------------------------------------------------------------------------------

상속(Inheritance)

서로 다른 여러개의 하위 클래스를 하나의 상위 클래스로 묶어서 사용
extends 키워드 사용
메소드 확장의 개념
상속받을 때 인스턴스도 함께 상속받음
